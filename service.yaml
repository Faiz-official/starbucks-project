apiVersion: v1
kind: Service
metadata:
  name: starbucks-service
  labels:
    app: starbucks-app   
spec:
  selector:
    app: starbucks-app
  ports:
  - name: http     
    port: 80
    targetPort: 80